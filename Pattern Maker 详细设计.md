# Pattern Maker 详细设计

> 状态：讨论敲定稿  
> 日期：2026-07-20  
> 适用语言：英语、日语  
> 目的：作为下一版 Pattern Maker、数据结构和识别器重做的共同依据。

## 1. 产品目标

Pattern Maker 是一个从 Obsidian 笔记选区启动的临时标注工作区。用户先从真实文本中制作一个准确的 Pattern 实例，再主动决定哪些条件必须保留、哪些条件可以抽象放宽。

Maker 不是 Pattern 资料编辑器，也不是一个要求用户填写大量字段的表单。它只负责：

1. 确定文本的语言和识别坐标。
2. 在原文中标记 Pattern 的具体元素。
3. 确定元素之间的结构关系。
4. 调整元素与关系的抽象程度。
5. 保存结构化 Pattern。

语言是必选项，因为英语和日语需要不同的句子、分句和 token 识别规则。Tag、说明、领域和语体等属于 Pattern 资料，不参与结构识别，允许为空；保存结构后可在一个轻量资料浮层中当场补充，也可跳过。

## 2. 核心原则

### 2.1 Pattern 的本体是元素，不是句子

Pattern 始终由具体元素 A、B、C 等构成。句子、分句、段落和文本行只负责描述元素出现在哪里、相距多远。

错误模型：

```text
A = 第一句
B = 第二句
C = 第三句
```

正确模型：

```text
A = 第一句中的“しか”
B = 第二句中的“しか”
C = 第三句中的“思う”
```

句子只是 A、B、C 的相对坐标。

### 2.2 先记录实例，再进行抽象

Maker 打开时保留完整原始选区。用户对实例的每一次拖选或边界选择，都先记录准确位置；随后才调整：

- 元素是否必须原样出现。
- 元素是否允许词元、屈折或词类层面的变化。
- 元素必须位于同句、相邻分句或后续若干句。
- 边界标点只是结构边界，还是 Pattern 的必需元素。

任何自动分析都只能提供候选，不应替用户决定最终抽象。

### 2.3 “模式”只是 UI 预设

Maker 提供四个容易理解的入口，但底层不建立四套互不兼容的数据结构：

1. 普通句内
2. 句内分句
3. 跨句
4. 同行窗口

它们都是“范围、坐标单位、元素、边界和关系”的不同组合。这样将来可以表达“跨句，同时要求 A 位于上一句最后一个分句”，而不需要不断增加新的 Pattern 类型。

### 2.4 语义分类全部交给 Pattern Tag

Idiom、搭配、术语、领域、修辞功能、语体和场景等不是 Maker 的结构类型。它们统一通过普通 Pattern Tag 管理。Tag 使用 `/` 表示层级，允许 Pattern 无 Tag 保存。

## 3. 文本坐标层级

语言文本按以下层级组织：

```text
文档
└─ 段落
   └─ 句子 S0、S1、S2……
      └─ 分句 C0、C1、C2……
         └─ 内容元素 A、B、C……
```

Markdown 原文行是另一种稳定坐标，专供同行窗口 Pattern 使用。它指文件中的真实文本行，不是编辑器窗口变窄后产生的视觉换行。

坐标编号只用于当前实例和关系表达。识别时不要求元素永远出现在绝对的“第1句”，而是保存相对关系，例如：

```text
A 所在句 → B 所在句：中间隔 0 句
B 所在句 → C 所在句：中间可隔 0～不限句
```

## 4. 三种元素角色

### 4.1 内容元素

用户在分句或句子内部拖选的连续文字，例如：

```text
A = しか
B = しか
C = 思う
```

一个内容元素本身必须是连续原文。断开的内容应拆成多个元素，而不是让一个元素内部再包含多段范围。

同样的表面文字可以出现多次，并作为不同元素保存。A 和 B 都是 `しか` 时，识别器必须找到两个不同的实际出现；一个 `しか` 不能同时充当 A 和 B。

第一版不允许元素范围互相重叠。例如 `思う` 与 `と思う` 不能同时作为两个重叠元素；后续如确有需要再设计嵌套元素。

### 4.2 边界元素

逗号、分号、冒号、破折号等可以先承担分句边界作用，也可以被用户提升为 Pattern 的必需元素。

例如：

```text
这是大胆的；我来了
```

只将分号用作边界时：

```text
[任意分句] — [边界不限] — [任意分句]
```

将分号提升为 Pattern 元素时：

```text
[任意分句] — [必须是；] — [任意分句]
```

标点默认严格匹配：

```text
； ≠ ;
— ≠ ——
—— ≠ ──
```

全角、半角、标点种类和重复数量不自动等价。只有用户主动添加备选形式时才放宽。该严格性服务于输出训练：用户在写作时应注意准确形式。

### 4.3 空坐标槽

分句或句子可以作为必须存在的结构槽，但内部没有指定内容元素。UI 中显示为“任意分句”或“任意句”，而不是显示为空白值。

允许保存纯结构 Pattern：

```text
[任意非空分句] — [必须是；] — [任意非空分句]
```

两侧分句必须包含实际文字；只有标点而没有两侧内容，不构成两个分句。

## 5. 四种 Maker 模式

### 5.1 普通句内

范围限定为一个语言学句子，不使用分句坐标。用户直接在句子中拖选 A、B、C。

- 只选择一个元素时，自然形成连续 Pattern。
- 选择多个元素时，自然形成多元素 Pattern。
- 不再要求用户预先选择“连续片段”或“断开组合”。
- 元素可以要求固定顺序。
- 元素之间可按字符距离或将来的 token 距离限制。
- 所有元素必须处于同一个句子中。

示意：

```text
…… [A not so much] …… [B as] ……
```

### 5.2 句内分句

总范围仍是一个句子，但使用 clause 作为坐标。制作过程分成“边界”和“元素”两个阶段。

#### 阶段一：确定分句边界

Maker 根据当前语言突出显示逗号、分号、冒号、破折号等可选标点。用户可以手动多选标点，确定当前实例如何划分：

```text
C0 这是大胆的 [；] C1 我来了 [——] C2 但事情还没有结束
```

被选中的标点默认只是边界，不自动成为 Pattern 必需元素。

#### 阶段二：选择元素并调整边界抽象

用户在各个分句内拖选内容元素，也可以把某个边界提升为严格元素：

```text
[C0：A 大胆]
— [边界不限] —
[C1：B 来]
— [必须是——] —
[C2：任意]
```

每个边界独立设置，不要求整个 Pattern 全部严格或全部宽松。

“边界不限”在识别时采用当前语言所有合法句内分句边界的最大析取：

```text
逗号 ∨ 分号 ∨ 冒号 ∨ 破折号 ∨ 其他被语言规则认可的句内分句边界
```

句号等句末边界不进入这个析取，因为 Pattern 的总范围仍是一个句子。

分句关系沿用跨句关系逻辑：

```text
A → B：紧邻下一个分句
B → C：中间可隔 0～1 个分句
```

### 5.3 跨句

用户在多个句子内部选择具体元素，而不是点击整句作为元素。

原始实例：

```text
S0 『ハイウエスト』には『ローライズ』[A しか]。
S1 『ローライズ』には『ハイウエスト』[B しか]。
S2 見えていないんじゃないかと[C 思う]──……
```

可以保存为严格连续关系：

```text
A → B：中间隔 0 句
B → C：中间隔 0 句
```

也可以放宽为：

```text
A → B：中间必须隔 0 句
B → C：中间可隔 0～不限句
```

“中间隔 0 句”表示元素所在句紧邻，避免使用含义容易混淆的“句距为1”。

跨句 Pattern 默认不能越过空行，即限定在同一段落。高级设置可以允许跨段；标题、不同列表项和不同引用块第一版视为硬边界。

跨句模式允许多个元素位于同一句，另一个元素位于后续句：

```text
S0：A …… B
S1：C
```

如果所有元素最终都限定在同一句，结构应归为普通句内或句内分句，而不是伪装成跨句 Pattern。

### 5.4 同行窗口

这是宽松共现模式。多个元素只要出现在同一个 Markdown 原文行，并落在一个有限字符窗口内，即可命中。

```text
元素：A、B、C
范围：同一原文行
窗口：不超过 30 字
顺序：固定或不限
```

窗口长度定义为：

> 从最早元素的开头到最晚元素的结尾，整个最小覆盖范围的长度。

元素自身长度包含在窗口内。第一版字符计数包含空格和标点，并以用户感知字符为目标，而不是简单使用内部编码单元。

英语第一版也可先按字符计算；以后增加“词数”单位。顺序只提供“固定”和“不限”两种，不在第一版实现部分顺序。

同一行出现大量重复元素时，正式命中数采用跨度最小、互不重叠的有效窗口，避免组合数量爆炸。

## 6. 元素间关系

每一对相邻元素或坐标槽拥有自己的关系，而不是只给整个 Pattern 设置一个含糊的最大距离。

### 6.1 句子和分句关系

统一保存：

- 是否同一坐标单位。
- 是否按顺序。
- 中间最少可隔多少单位。
- 中间最多可隔多少单位；可为不限。

UI 使用自然语言胶囊，不直接暴露四个表单：

```text
[同一分句]
[紧邻下一分句]
[后方0～1分句]
[后方任意分句]

[同一句]
[紧邻下一句]
[后方0～2句]
[后方任意句]
```

点击胶囊后才出现最小值、最大值等详细设置。

### 6.2 字符窗口关系

同行窗口采用整体最小覆盖窗口，而不是逐对距离。否则 A→B 与 B→C 都接近上限时，整个 Pattern 会被无意拉得很长。

### 6.3 元素必需性

第一版所有内容元素和被提升的边界元素都必须命中。可选元素、否定元素和通配槽属于未来能力，不能通过悄悄忽略元素来模拟。

## 7. 英语与日语的边界识别

### 7.1 语言必须先于坐标分析

选择语言后，Maker 才能计算句子和分句候选。改变语言会重新计算边界；若已经存在依赖边界的标注，Maker 必须明确提示并要求确认，不能静默破坏标注。

语言下拉未选择时显示“选择语言”，但该提示不能作为与“英语、日语、其他”并列的可选项。选择后控件直接显示当前语言，例如“日语”。模式下拉同理。

### 7.2 英语句子规则

第一版需要处理：

- `. ? !` 等终止标点。
- 终止标点与右引号、右括号的组合。
- 常见缩写，如 `e.g.`、`Dr.`。
- 小数点。
- 省略号。
- 引号或括号中的标点。
- 没有终止标点的最后一段非空文字。

### 7.3 日语句子规则

第一版需要处理：

- `。！？`。
- `。」`、`？」` 等与右引号的组合。
- `……`、`──`。
- 日文中混入的拉丁字母和英文句点。
- 引号中的句子。
- 没有终止标点的最后一段非空文字。

### 7.4 分句候选与最短长度

逗号不能机械地等同于 clause。语言规则先生成候选，再结合长度和基础排除规则判断可信度。

日语初始可按有效字符数筛选，建议以 6～8 字作为需要通过真实材料调整的起点。英语按词数而不是字母数筛选，建议以 3～4 词为起点。空格和标点不计入这里的“有效长度”。这些阈值属于语言设置，不常驻 Maker 主界面。

长度只是候选证据，不能成为绝对规则：

- `If so, we should leave.` 很短但可能有真实分句。
- 长名词列举可能很长但不是多个 clause。

第一版候选判断综合：

- 两侧是否达到基本长度。
- 是否为明显列举。
- 是否位于括号或引号内部。
- 标点强度：分号、冒号通常高于逗号，破折号可能是插入语。
- 用户是否手动确认。

以后接入语言分析器后再加入谓语、限定动词、连接词和句法完整性证据。

Maker 中可将高可信边界显示为正常分隔线，低可信边界显示为很淡的候选。用户点击即可确认或取消，也可在允许的位置手动添加边界。制作与后续扫描必须使用同一套有版本的语言边界规则，避免坐标不一致。

## 8. 元素匹配规则

### 8.1 第一版正式能力

第一版只开放真正可工作的“原文匹配”。不再显示尚未实现的“语法等价”选项。

正式命中默认严格处理：

- 英文大小写。
- 标点种类。
- 全角与半角。
- 标点重复数量。
- 标准空格位置。

将来可以把不规范空格或大小写显示为“近似命中／书写提醒”，但不能计入正式命中。

### 8.2 为未来保留的元素抽象

数据结构应能扩展到：

- 词元及常见屈折变化。
- 屈折变化加时态、肯否、语体等限制。
- 词类与语法特征。
- 用户指定的备选表达。
- 明确的通配槽。

多词元素需要在 token 化后允许用户指定哪个内部词是可变化中心。日语和英语使用不同的分析器，不能只做字符串替换。语义近义词不与词形变化混为一谈。

## 9. Maker UI

### 9.1 总体布局

Maker 是一个以原文画布为中心的轻量工作区：

```text
[日语 ▾] [跨句 ▾]                         [撤销图标] [清空图标]

原文／句子／分句画布

[A しか｜原文] — [紧邻下一句] — [B しか｜原文] — [后方任意句] — [C 思う｜原文]

                                                     [取消] [保存]
```

主界面不放教程段落、编号大卡片和大量文字按钮。解释放在文档、设置说明和悬停提示中。

### 9.2 顶部控件

- 语言和模式使用紧凑下拉。
- 下拉提示项不可选择，不与真实选项并列。
- 已选择时只显示当前值。
- 撤销、清空等辅助操作使用图标与 tooltip。
- 危险或低频操作不占据视觉中心。

### 9.3 画布

- 普通句内：显示一个句子文本画布。
- 句内分句：显示 C0、C1、C2 的淡坐标和可点击边界。
- 跨句：显示 S0、S1、S2 的淡坐标，但用户仍在句内拖选具体文字。
- 同行窗口：按 Markdown 原文行显示。
- 第一次拖选生成 A，第二次生成 B，依次类推。
- 当前元素使用清晰但克制的强调色。
- 不使用会遮盖大段文字的强烈背景。

### 9.4 元素与关系胶囊

元素胶囊：

```text
[A しか｜原文]
[B 思う｜原文]
[；｜严格]
[C1｜任意分句]
```

关系胶囊：

```text
[紧邻下一句]
[后方0～1句]
[边界不限]
[窗口≤30字]
[顺序不限]
```

点击胶囊打开小型弹出层修改详细条件。元素可通过拖动胶囊重排；删除使用键盘 Delete 或胶囊菜单，不常驻红色删除按钮。

### 9.5 句内分句的两阶段操作

只显示两个轻量阶段标签：

```text
边界 → 元素
```

边界阶段允许多选标点、确认分句。元素阶段允许在各分句内拖选 A、B、C，并将某些边界提升为必需元素。用户可以随时返回边界阶段，但修改边界若会影响已有元素，必须预览影响并要求确认。

### 9.6 保存后的资料浮层

Maker 保存结构后出现轻量资料浮层：

- Tag：可选，支持 `/` 路径。
- 说明：可选。
- 跳过。

已有 Pattern 的资料编辑器与 Maker 分开。资料编辑器只处理语言、Tag、说明等元信息；需要修改结构时，再明确进入 Maker。

## 10. 识别与命中

### 10.1 动态扫描

Pattern 不向笔记正文写入专用元数据。插件根据当前笔记内容即时扫描并刷新命中，类似搜索。

原始来源与动态命中是两类信息：

- 原始来源：最初制作 Pattern 的文件与选区。
- 当前命中：插件此刻在 Vault 或当前笔记中重新识别出的实例。

“跳到原始来源”和“查看当前命中”应作为不同操作。

### 10.2 候选组合

当同一范围中有多个可能的 A、B、C 时：

1. 找出满足全部元素和关系的组合。
2. 优先跨度最小的组合。
3. 跨度相同时优先位置最早的组合。
4. 同一实际文本范围不能同时充当两个元素。
5. 正式计数采用互不重叠的组合。

高级检查页以后可以展示所有候选，但日常命中数不能因排列组合而膨胀。

### 10.3 高亮

复杂 Pattern 只分别高亮 A、B、C 和被要求的边界标点，不高亮它们之间的全部文字。属于同一命中的元素使用一致编号或视觉联系。点击一个命中时滚动到第一个元素，并短暂强调同组的其他元素。

### 10.4 近似书写提示

第一版可先不实现。未来可把大小写、空格或标点不规范但结构接近的实例显示为“近似”，用于输出纠错；近似实例不计入正式命中。

## 11. 数据结构要求

具体字段名可在实现时调整，但必须表达以下概念：

```text
Pattern
├─ language：英语／日语／其他
├─ scope：原文行／句子／段落／更大范围
├─ coordinateUnit：文字／分句／句子／字符窗口
├─ sourceSnapshot：制作时的原文快照
├─ slots：句子或分句坐标槽
├─ components：内容元素与边界元素
├─ relations：元素或槽之间的相对关系
├─ window：同行窗口条件
└─ segmentationProfile：语言边界规则版本
```

内容元素至少保存：

```text
id
显示标签 A/B/C
原文内容
在来源快照中的准确范围
所属句子与分句坐标
匹配方式
未来语言学限制
```

边界元素至少保存：

```text
准确标点
来源范围
边界两侧坐标槽
仅作边界／Pattern必需元素
严格匹配／未来备选集合
```

关系至少保存：

```text
前项与后项
坐标单位
顺序要求
最少间隔
最多间隔或不限
是否要求同一坐标单位
```

同行窗口单独保存：

```text
同一Markdown原文行
最大字符窗口
顺序固定／不限
```

数据必须放在当前 Vault 的 `_PatternOutputTracker/` 中。笔记正文保持干净。

## 12. 来源稳定性与编辑

保存 Pattern 时记录：

- 文件路径。
- 原文快照。
- A、B、C 和边界的快照内位置。
- 少量前后上下文。

原笔记修改后，插件先通过原文和上下文在附近重新定位。无法定位时保留来源记录，并显示“来源已变化”，不能因此删除 Pattern。

重新编辑结构时优先读取原始来源的当前内容；若来源已不存在，使用保存快照打开 Maker。

相同文字但关系条件不同的 Pattern 允许分别保存。第一版将严格版与宽松版视为不同 Pattern；Pattern 家族以后再设计。

## 13. 第一版实施范围

第一版重做应实现：

- 正确的通用数据模型。
- 普通句内、句内分句、跨句、同行窗口四个入口。
- 英语、日语基础切句。
- 标点候选与人工分句。
- 内容元素、边界元素和空坐标槽。
- 分句／句子之间逐段设置关系。
- 同行字符窗口。
- 严格原文匹配。
- 动态命中、高亮与计数。
- Maker 与资料编辑器完全分离。
- Vault 内数据保存及旧数据迁移。

第一版明确不伪装实现：

- 词元和屈折识别。
- 词类槽。
- 语义近义表达。
- 可选元素与否定元素。
- 通配槽。
- 完整句法 clause 分析。
- Pattern 家族。
- 复杂的跨段篇章 Move 分析。

这些能力可以在数据结构中预留，但 UI 中不出现不能工作的选项。

## 14. 核心验收实例

### 14.1 日语跨句

来源：

```text
『ハイウエスト』には『ローライズ』しか。
『ローライズ』には『ハイウエスト』しか。
見えていないんじゃないかと思う──視線上にありながら、まったく無視されているみたいな気分だった
```

标注：

```text
A = S0 中的 しか
B = S1 中的 しか
C = S2 中的 思う
```

抽象：

```text
A→B：中间隔0句
B→C：中间可隔0～不限句
全部元素严格原文
```

### 14.2 句内分句与严格边界

来源：

```text
这是大胆的；我来了
```

可保存为：

```text
[任意非空分句] — [；·严格] — [任意非空分句]
```

`；` 与 `;` 不等价。

### 14.3 句内分句与宽松边界

来源：

```text
C0：包含A；C1：包含B——C2：包含C
```

可保存为：

```text
[C0：A] — [边界不限] — [C1：B] — [——·严格] — [C2：C]
```

第一个边界可由语言规则认可的逗号、分号、冒号或破折号满足；第二个边界只能是准确的 `——`。

### 14.4 同行窗口

```text
A、B、C位于同一Markdown原文行
最小覆盖窗口≤30字符
顺序不限
```

如果元素跨越真实换行，或者最小覆盖窗口超过30字符，则不命中。

## 15. 最终设计判断

下一版 Maker 的重点不是增加更多按钮，而是让用户清楚地操作两种抽象：

1. 元素抽象：A、B、C 和边界本身允许怎样变化。
2. 关系抽象：这些元素可以出现在哪个句子、分句或文字窗口中，相隔多远。

句子与分句是坐标，文字和被指定的标点才是 Pattern 元素。Maker 必须始终让原文实例、当前坐标和抽象后的关系同时可见，但只显示当下真正需要的控件。
