# Pattern 输出追踪插件：产品与开发规划

> 状态：讨论中的活文档  
> 创建日期：2026-07-19  
> 用途：持续记录产品哲学、关键决策、开放问题、开发阶段和验证结果。讨论中形成的新决定应更新到这里，而不是散落在聊天记录中。

## 1. 一句话定义

一个面向高水平英语、日语写作者的 Obsidian 插件：从阅读与分析笔记中采集可复用的语言 pattern，将其连接到用户既有的修辞 tag 体系，并在实际写作中帮助调用、识别和追踪这些 pattern 是否进入了主动产出能力。

它不是生词本、翻译器或间隔复习工具。核心问题不是“我是否记得这个表达”，而是：

> 我收集过的表达和修辞结构，后来是否真的进入了自己的写作？是在无提示下自然使用，还是只能借助检索和插入？

## 2. 产品原则

1. **输出优先**：收集只是入口，写作中的调用与 hit 才是产品核心。
2. **保留语言复杂性**：不能把 pattern 限制为连续短语；最终要容纳断开搭配、跨句关系和句组级修辞结构。
3. **接入既有思维体系**：Obsidian tag 是重要检索入口。插件不要求用户重新建立一套孤立分类法。
4. **区分辅助使用和主动掌握**：自然 hit、检索后 hit、插入后 hit、目标练习 hit 的意义不同，不能简单合并成一个出现次数。
5. **本地、可读、可迁移**：用户数据不应被某个 NLP 库或索引格式绑架。引擎可以替换，pattern 数据应长期可读。
6. **渐进增强**：没有日语分词或英语词形归一引擎时，精确匹配仍能工作；高级引擎是在核心闭环之上的增强。
7. **先验证语义，再扩充功能**：管理页、推荐系统、统计图表都不能先于“录入—识别—回到出处”这条闭环。

## 3. 核心闭环

```text
阅读/分析笔记
    ↓ 选中并录入
Pattern 库
    ↓ 通过文字、插件 tag、Obsidian tag 调用
写作
    ↓ 自动扫描并识别
Occurrence / hit
    ↓ 区分自然、检索辅助、插入、目标练习
主动产出能力反馈
```

## 4. Pattern 的范围

长期目标中的统一模型需要覆盖四个层级：

| 层级 | 示例 | 初步表达方式 | MVP |
|---|---|---|---|
| 连续片段 | `in light of` | 单一 slot | 是 |
| 断开 pattern | `take ... into account` | 多 slot + 顺序、间距约束 | 紧随 MVP |
| 跨句/段内 pattern | A、B 出现在同段或相邻句 | 多 slot + scope 约束 | 后续 |
| 句组级修辞 move | 让步 → 限定 → 主张 | 句子角色序列 | 研究性阶段 |

需要坚持“统一概念模型”，但不必在第一个版本中同时实现全部匹配器。第一版先用最简单的连续片段验证整个闭环，同时确保数据结构不会堵死后续层级。

## 5. 四类关键对象

### 5.1 Pattern

代表用户希望积累和复用的语言形式或修辞结构。

初始字段草案：

```ts
interface Pattern {
  id: string;
  language: "en" | "ja";
  kind: "contiguous" | "discontinuous" | "cross-sentence" | "move-sequence";
  displayText: string;
  slots: PatternSlot[];
  note?: string;
  pluginTagIds: string[];
  obsidianTags: string[];
  sources: SourceRef[];
  createdAt: string;
  updatedAt: string;
}
```

### 5.2 TagConcept

插件内部可评论、可解释的概念节点。它与 Obsidian tag 有联系，但两者不强制一一对应。

```ts
interface TagConcept {
  id: string;
  name: string;
  description?: string;
  aliases: string[];
  linkedObsidianTags: string[];
  createdAt: string;
  updatedAt: string;
}
```

暂定支持三种关系：

- pattern 直接关联 Obsidian tag；
- pattern 归入插件 TagConcept，TagConcept 再关联 Obsidian tag；
- 从 Obsidian tag 一键创建同名 TagConcept，但不自动强制创建。

### 5.3 InvocationEvent

代表用户在写作中主动寻找或插入 pattern 的过程。它是区分“自然产出”和“提示后产出”的必要证据。

```ts
interface InvocationEvent {
  id: string;
  patternId?: string;
  filePath: string;
  query?: string;
  queryObsidianTags: string[];
  action: "view" | "copy" | "insert" | "pin-as-target";
  createdAt: string;
}
```

### 5.4 Occurrence

代表一次被识别出的实际出现。Occurrence 不能只保存数量，必须能回到上下文。

```ts
interface Occurrence {
  id: string;
  patternId: string;
  filePath: string;
  blockId?: string;
  from: number;
  to: number;
  matchedText: string;
  confidence: number;
  mode: "natural" | "searched" | "inserted" | "targeted" | "unknown";
  detectedAt: string;
}
```

位置是否长期使用字符 offset 仍待验证。Markdown 编辑后 offset 会漂移；较可靠的组合可能是文件路径、block id、上下文指纹以及可重算的扫描结果。

## 6. 第一个纵向切片（Milestone 0）

目标不是发布可用产品，而是验证这个插件最关键的技术路径。

### 用户流程

1. 在专用测试 vault 的编辑器中选中一段连续英文或日文。
2. 运行命令“Capture selection as pattern”。
3. 弹出最小录入窗口：确认文本、语言，可选填写一个 tag 或备注。
4. 保存 pattern，记录当前文件和 block/上下文作为来源。
5. 扫描当前文档，对完全相同的文本进行精确匹配。
6. 在编辑视图中轻量标记命中位置。
7. 点击或悬停命中，能看到 pattern 信息并跳回来源。

### 这个切片要回答的问题

- 在 Obsidian 当前编辑器中，选择、装饰和更新命中的可靠方式是什么？
- pattern 数据怎样落盘，既方便插件读取又方便用户备份与迁移？
- Markdown 变化后，Occurrence 的位置如何稳定重建？
- 对中英日混排文本，最基础的语言判断和精确匹配是否足够可用？
- 大量 pattern 下，何时需要索引，而不是每次全量遍历？

### 明确不做

- 英语 lemma 和日语活用归一；
- 断开 pattern 编辑器；
- 全 vault 后台扫描；
- 完整管理页面；
- 统计图表和掌握度评分；
- 上下文智能推荐；
- AI 或外部服务。

## 7. MVP 边界（Milestone 1）

Milestone 0 技术验证通过后，才进入可日常试用的 MVP：

1. 连续 pattern 的快速录入、编辑和删除；
2. 来源记录与跳转；
3. 当前文档或指定“输出笔记”的精确扫描；
4. 命中高亮与 occurrence 列表；
5. 最小 pattern 搜索框；
6. plugin tag 与 Obsidian tag 的关联和 `#` 补全检索；
7. 从检索结果插入连续 pattern；
8. 区分至少 `natural` 与 `inserted/searched`；
9. 数据导出或直接可备份的数据文件；
10. 基础设置：输入/输出文件范围、扫描开关、数据位置。

MVP 的成功标准不是功能数量，而是用户能真实完成：

> 收集 20–50 个 pattern，在数篇写作中通过既有修辞 tag 找到它们，并可靠看到哪些是自然使用、哪些是辅助使用。

## 8. 后续阶段

### Milestone 2：断开 pattern

- 选中较大上下文，在 modal 中二次标记多个 slot；
- slot 顺序、最大距离、同句/同段约束；
- 英语词形归一；
- 日语分词与活用归一；
- 命中置信度和误报确认。

### Milestone 3：练习与反馈

- 本篇目标 pattern 清单；
- searched / inserted / targeted / natural 的完整事件关联；
- “收藏但未使用”“只能提示后使用”“逐渐自然化”等反馈；
- 允许用户确认、否认和修正自动 hit。

### Milestone 4：跨句和修辞 move

- 句子切分和段落 scope；
- A → B → C 的结构模板；
- 以人工标注、半自动识别为主，不假设一开始就能可靠自动理解修辞功能；
- 研究句组 pattern 是“形式模板”“功能模板”还是两者组合。

## 9. 技术起步方案

采用 Obsidian 官方 sample plugin 作为工程基线，使用 TypeScript 和官方 `obsidian` API。开发必须在独立测试 vault 中进行，不能直接在主 vault 试验。

建议的初始目录：

```text
src/
  main.ts
  domain/
    pattern.ts
    occurrence.ts
    tag-concept.ts
  application/
    capture-pattern.ts
    scan-document.ts
  infrastructure/
    storage/
    obsidian/
  ui/
    capture-modal.ts
    decorations.ts
tests/
  domain/
  fixtures/
docs/
```

分层意图：

- `domain`：定义“什么是 pattern、如何匹配”，不依赖 Obsidian UI 和具体 NLP 库；
- `application`：组织录入、扫描、调用等用例；
- `infrastructure`：Obsidian API、存储和未来 NLP 引擎适配；
- `ui`：modal、侧边栏和编辑器装饰。

早期不要引入大型 UI 框架、数据库 ORM 或复杂状态管理。先使用 Obsidian 原生组件和简单 TypeScript 数据结构。

## 10. 存储策略：待用原型验证

目前有三个候选方案：

| 方案 | 优点 | 风险 |
|---|---|---|
| `plugin data.json` | 实现最简单、读写集中 | 用户不易直接浏览；大规模 occurrence 可能膨胀 |
| 每个 pattern 一个 Markdown 文件 | 最符合 Obsidian、可链接可搜索 | 文件数量多；schema 演化和频繁统计较麻烦 |
| Markdown/JSON 混合 | pattern 资产可读，索引与事件高效 | 同步规则更复杂 |

当前建议：Milestone 0 先使用版本化 JSON 验证对象和迁移机制，同时保持领域模型与存储格式解耦。在真实使用 20–50 条后，再决定 pattern 是否需要成为独立 Markdown 一等公民。不要在没有使用证据前把存储哲学定死。

无论采用哪种方案，都应包含：

- `schemaVersion`；
- 可重建的索引，不把索引当唯一事实来源；
- 原始 pattern 与归一形式分开保存；
- 数据迁移函数；
- 导出与备份路径。

## 11. 匹配架构

核心层只依赖窄接口：

```ts
interface TextAnalyzer {
  analyze(text: string, language: "en" | "ja"): AnalyzedText;
}

interface PatternMatcher {
  find(pattern: Pattern, text: AnalyzedText): Match[];
}
```

演进顺序：

1. 原文精确匹配；
2. 大小写、Unicode、空白和标点归一；
3. 英语 lemma；
4. 日语 tokenization 与基本形；
5. 多 slot 和 scope 约束；
6. 可解释的模糊匹配。

任何一次 hit 都应能说明“为什么命中”，而不是只给一个不可解释的相似度。

## 12. 测试策略

### 纯逻辑测试

- pattern schema 与迁移；
- 精确匹配和归一规则；
- invocation 到 occurrence 模式的判定；
- tag 查询的 AND/OR、嵌套 tag 语义。

### Obsidian 集成测试

- 选择文本和 block 来源捕获；
- 编辑器更新后的 decoration 重算；
- 文件重命名、移动和删除；
- tag cache 更新；
- 插件卸载/重载后数据一致性。

### 人工语料夹具

建立小型固定语料，至少覆盖：

- 英文大小写、时态和标点变化；
- 日语活用、助词变化和无空格文本；
- 中英日混排；
- Markdown 链接、代码块、frontmatter 和嵌入内容；
- 同一 pattern 在一段中多次出现。

代码块、frontmatter、链接目标等区域是否参与扫描必须明确规定，不能让默认行为隐式决定。

## 13. 当前最重要的产品问题

1. 哪些笔记属于“输入”，哪些属于“输出”？通过文件夹、frontmatter、Obsidian tag，还是手动会话指定？
2. pattern 的来源是否允许多个？同一 pattern 从不同作者、语域和上下文中多次遇到时如何呈现？
3. plugin tag 与 Obsidian tag 的查询是默认并集还是交集？多个 tag 如何组合？
4. 检索结果插入后，多长时间/多近位置内的 hit 才归因于这次 invocation？
5. 用户手动修改插入文本后仍命中，应算 inserted、searched 还是逐渐独立产出？
6. 自然 hit 是否只统计“输出笔记”，避免阅读材料中的大量出现污染主动使用指标？
7. 对句组级修辞 move，自动识别应做到什么程度，哪些判断必须保留给用户？

## 14. 第一轮开发任务

在开始编码前，需要准备一个独立测试 vault。之后按顺序完成：

1. 从官方 sample plugin 建立本地工程并修改 manifest；
2. 加入最小测试框架和 lint/typecheck；
3. 定义版本化 `Pattern` 与 `SourceRef` schema；
4. 实现“Capture selection as pattern”命令和最小 modal；
5. 实现 JSON repository；
6. 实现当前文档的精确扫描；
7. 接入编辑器 decoration；
8. 加入来源跳转；
9. 用英语、日语各 10 条真实 pattern 进行人工试用；
10. 根据误报、漏报和操作摩擦更新本文档，再决定 MVP 的下一步。

## 15. 决策日志

### 2026-07-19：产品定位

- 用户是高水平英语、日语学习者，目标是高级写作输出，不是基础输入学习。
- 核心差异化是 pattern 使用追踪闭环和多粒度 pattern，而非单纯 phrasebank。

### 2026-07-19：tag 的作用

- 用户已有一套以 Obsidian tag 表达的修辞分析与思考体系。
- Obsidian tag 是 pattern 检索的主入口之一，而不是仅供参考的附属字段。
- 插件 TagConcept 可以有评论、定义、别名，并与一个或多个 Obsidian tag 建立映射。

### 2026-07-19：使用事件

- 不把所有出现合并成简单次数。
- 产品需要逐步区分自然 hit、检索后 hit、插入后 hit 和目标练习 hit。

### 2026-07-19：开发顺序

- 先完成连续文本的录入—保存—精确识别—来源跳转纵向切片。
- 断开 pattern、形态归一和句组级 move 后置，但基础 schema 需为其留出空间。

### 2026-07-19：Pattern 类型与分层 Pattern Tag（已被下一条决策修正）

- 这一阶段曾把结构 Pattern、搭配、Idiom／惯用语、固定用语、术语／领域用语和词语用法设为互斥条目类型。
- 实作后发现，这混淆了 pattern 的 syntactic／matching structure 和语义分类，并给录入增加了不必要的判断负担。
- Pattern Tag 与 Obsidian Tag 是两套系统；Pattern 直接关联 Pattern Tag，Pattern Tag 可以映射一个或多个 Obsidian Tag 名称。
- 系统中只有一种普通 Pattern Tag。Tag 可以没有父级，也可以拥有一个父级；分层只是普通 Tag 之间的父子关系。
- 不区分临时 Tag、正式 Tag、Domain Tag 或分层 Tag。“临时”只描述用户尚未整理的状态，不是数据类型。
- Domain 不作为 Pattern 的必填字段。用户可以自行建立“领域 > 经济学 > 金融”等普通 Tag 层级，也可以只使用顶层的“金融”。
- Pattern 可以关联多个 Tag；浏览父 Tag 时，应汇总所有子 Tag 下的 Pattern，但 Pattern 只保存它直接关联的 Tag。
- 第一版一个 Tag 最多有一个父级，避免过早引入多父级网状结构。

### 2026-07-19：恢复 syntactic structure；语义分类全部归入 Tag

- Pattern 的正式结构分类恢复为最初模型：`contiguous`、`discontinuous`、`cross-sentence`、`move-sequence`。
- 结构类型描述 Pattern 怎样组成以及插件怎样识别，不描述其语义、词汇性质或使用场景。
- Idiom、搭配、固定用语、术语、领域、修辞功能、语体、场景等分类全部使用普通 Pattern Tag。
- 结构类型尽量由标注行为自动产生。普通选中文字默认是 `contiguous`，用户不需要填写类型下拉框。
- 新建 Pattern 只要求确认内容和语言；Tag、说明等信息可以为空。
- Pattern Tag 使用 `/` 输入层级路径，例如 `领域/经济学/金融`；按 Enter 添加，保存时创建不存在的层级。
- 完整 Tag 路径唯一，但不同父级下面可以有相同名称的末级 Tag。
- Tag 管理页保留用于搜索、重命名、移动、删除、说明、别名及 Obsidian Tag 映射；日常收集不依赖树状管理 UI。
- 0.3.0 的语义 `kind` 在迁移时转换成 `表达性质/...` 普通 Tag，现有 Pattern 的实际识别结构统一迁移为 `contiguous`。

## 16. 官方开发参考

- Obsidian Developer Docs — Build a plugin: <https://docs.obsidian.md/Plugins/Getting%20started/Build%20a%20plugin>
- Obsidian official sample plugin: <https://github.com/obsidianmd/obsidian-sample-plugin>
- Obsidian API type definitions: <https://github.com/obsidianmd/obsidian-api>

## 17. UI 头脑风暴（未定案）

### 17.1 UI 的四个发生时刻

UI 不应围绕一个庞大的“管理页面”组织，而应围绕用户所在的四个时刻组织：

| 时刻 | 用户注意力 | 合适的 UI | 首要原则 |
|---|---|---|---|
| 采集 Capture | 正在阅读和分析 | 快速命令、录入 modal | 少打断上下文 |
| 调用 Recall | 正在写作、寻找表达 | 光标附近搜索框、写作侧栏 | 键盘优先、按意图检索 |
| 命中 Observe | 刚刚写出或修改文本 | 编辑器轻量标记、popover | 确认而不打扰 |
| 整理 Curate | 专门维护 pattern 库 | 高密度三栏管理视图 | 比较、筛选、批量维护 |

### 17.2 采集 UI：快路径与精细路径

#### 快路径

适合连续 pattern：

1. 选中文字；
2. 执行快捷命令；
3. 右上角出现可撤销的轻提示，例如“已收集：in light of”；
4. 提示中提供“补充信息”入口，而不是强制填写 modal。

目标是让高频采集接近一次操作。语言、来源、创建时间和当前笔记 tag 可以自动推断或继承。

#### 精细路径

适合断开 pattern、跨句结构以及需要立即整理的条目。建议使用“标注画布 + 属性区”，而不是多步 wizard：

```text
┌─ Capture pattern ───────────────────────────────┐
│ Source context                                  │
│ It is [not so much] a failure [as] a redirection│
│        └ slot A ┘           └B┘                 │
│                                                 │
│ Preview     not so much ⟨X⟩ as ⟨Y⟩              │
│ Scope       same sentence   Distance  ≤ 12 tokens│
│ Match       exact / normalized / lemma          │
│ Language    English                              │
│ Tags        #修辞/重心转移  @contrast            │
│ Note        …                                    │
│                              Cancel   Save       │
└─────────────────────────────────────────────────┘
```

其中 `#` 表示 Obsidian tag，`@` 暂表示插件 TagConcept；最终符号仍待验证，不能让查询语法增加记忆负担。

### 17.3 写作调用 UI：两种入口，共用同一搜索核心

#### 光标附近的快速调用器

快捷键打开，体验接近 Quick Switcher：

```text
┌─ Find a pattern ────────────────────────────────┐
│ #修辞/让步  lang:ja                             │
├─────────────────────────────────────────────────┤
│ とはいえ                         natural 2       │
│ もっとも、…というわけではない   assisted 4      │
│ Xを認めつつも、Y                  structure       │
└─────────────────────────────────────────────────┘
  Enter 插入 · 另一动作钉为本篇目标 · 打开查看出处
```

查询应同时支持：

- 普通文字和 pattern 原文；
- `#` 触发 Obsidian tag 补全；
- 插件 TagConcept；
- 语言、pattern 类型、使用状态等过滤器；
- 多个 tag 的交集/并集，但默认语义仍待确定。

#### 常驻写作侧栏

侧栏不是完整数据库，而是当前写作的“工作台”：

```text
┌─ Writing palette ───────────────┐
│ [ search by idea or #tag… ]     │
│                                 │
│ 本篇目标  2 / 5                 │
│ ✓ in light of                   │
│ ✓ とはいえ                      │
│ ○ not so much X as Y            │
│ ○ たとえXてもY                  │
│                                 │
│ 最近调用                        │
│ …                               │
│                                 │
│ 当前上下文建议（后续）          │
│ …                               │
└─────────────────────────────────┘
```

目标清单应该克制，不做强游戏化。它是写作工作记忆的外部支架，而不是打卡系统。

### 17.4 搜索结果卡片应显示什么

搜索结果必须足够紧凑，但要帮助用户在相似表达之间做选择。推荐分两层：

- 第一行：pattern 骨架、语言、类型；
- 第二行：最关键的功能 tag、短备注或一个代表性来源片段；
- 辅助信息：自然 hit / 辅助 hit 次数、最近使用；
- 展开后：完整例句、来源、语域、匹配规则和评论。

不建议默认使用大卡片瀑布流；高水平用户的 pattern 库可能很密，需要快速纵向比较。

### 17.5 编辑器中的 hit 反馈

命中标记应比拼写检查更轻，避免把写作页面变成彩色标注页：

- 默认使用细下划线、背景淡色或 gutter 小点中的一种；
- hover/click 后才显示 pattern 名称、使用类型、历史次数和来源；
- 同一句有多个重叠 pattern 时，在 popover 中合并展示，避免叠加多层颜色；
- 提供当前文件临时隐藏、全局关闭和只显示目标 pattern 三种可见性；
- 自动判定应允许用户修正：“这是自然使用 / 我刚才检索过 / 不是有效 hit”。

命中类型不一定要在正文中使用四种颜色。正文只回答“这里命中了”，具体归因放在 popover 和详情页中，减少视觉噪音。

### 17.6 Pattern 库：高密度三栏视图

建议采用“筛选—列表—详情”而不是卡片墙：

```text
┌─ Facets ─────┬─ Patterns ─────────────────┬─ Inspector ────────┐
│ Language     │ Pattern       Nat  Assist  │ not so much X as Y │
│ □ English    │ in light of    7      1    │ contrast / focus   │
│ □ Japanese   │ not so much…   2      5    │                    │
│              │ とはいえ       9      0    │ Structure          │
│ Type         │ …                          │ slots / constraints │
│ Tags         │                            │                    │
│ Status       │                            │ Sources            │
│              │                            │ Occurrences        │
│              │                            │ Notes / tags       │
└──────────────┴────────────────────────────┴────────────────────┘
```

中栏可排序字段候选：

- 创建时间、最近使用；
- 自然 hit、辅助 hit；
- 来源数量；
- “从未使用”“只在提示后使用”等派生状态；
- 语言、类型和 tag。

右侧 Inspector 负责查看与编辑，减少频繁弹窗。复杂的 slot 标注可以再次打开精细编辑 modal。

### 17.7 TagConcept UI

不急于单独做“tag 管理中心”。早期可以先在 Pattern Inspector 中完成：

- 添加插件 TagConcept；
- 搜索并关联 Obsidian tag；
- 查看 tag 的定义、评论、别名和 pattern 数量；
- 检测已不再存在于 vault 的 Obsidian tag 字符串。

当 tag 数量足够大后，再考虑独立的映射视图。图谱可以作为探索工具，但不应成为主要维护界面；多对多映射通常用表格或双栏映射器更清楚。

### 17.8 句组级 pattern 的特殊呈现

句组级修辞 move 不适合像短语一样直接插入。它更像可钉在写作侧栏中的结构卡：

```text
让步后收回范围

1. 承认对方观察或局部事实
2. 限定其适用范围
3. 回到自己的核心主张

□ Move 1   □ Move 2   □ Move 3
[查看三个来源实例]
```

早期可以让用户手动确认各 move 是否完成；自动识别只提供候选，不伪装成可靠的修辞理解。

### 17.9 需要原型验证的 UI 决策

1. 连续 pattern 是否默认“一键保存”，还是始终先弹出轻量录入框？
2. 搜索结果选择后，默认动作是插入、复制、钉为目标，还是仅打开详情？
3. `#Obsidian tag` 与插件 TagConcept 在视觉和查询语法上如何区分？
4. hit 标记采用下划线、背景还是 gutter，哪一种与用户现有主题冲突最少？
5. 来源上下文在搜索结果中应显示多长，是否需要并排比较多个来源？
6. 本篇目标保存在哪里：写入笔记 frontmatter、插件会话数据，还是独立 practice session？
7. 侧栏在不写作时显示什么，还是应完全隐藏？
8. 首版是否明确 desktop-first？多选标注、hover 和高密度三栏在移动端需要完全不同的交互。

### 17.10 当前倾向

- desktop-first，但数据和基础搜索不阻断未来移动端；
- 快速采集和精细采集并存；
- 快速调用器与写作侧栏共用搜索、排序和事件记录；
- 编辑器反馈极轻，详细信息按需展开；
- 库管理使用三栏高密度布局；
- 先做连续 pattern 的三张低保真原型：采集 modal、调用器、hit popover，再进入实现。
# 0.5.0 决策补记：Pattern Maker 与资料编辑分离

Pattern Maker 是从笔记选区启动的临时结构标注工作区，不是已有 Pattern 的元数据表单。它先选择句法结构，再提供对应工具：连续范围、断开成分、句子序列或句组序列。语言、Tag、说明属于 Pattern 资料，保存在独立轻量编辑器中。

复杂 Pattern 采用“成分 + 约束”的组合模型：A/B/C 成分各自保存原文范围、单位与匹配方式；整体保存作用范围、顺序、邻接和最大距离。语义、领域、语体、Idiom、术语等继续统一由普通 Pattern Tag 管理。

界面不在工作区堆放教程式文字。主要操作依靠结构下拉、胶囊状态、图标工具和悬停提示；完整解释放在测试说明和文档中。
