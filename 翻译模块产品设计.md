# 翻译模块产品设计

> 本文整理截至当前讨论已经确认的产品模型与交互方向。页面名称只用于说明层级，不代表最终 UI 必须使用这些文案。

---

## 1. 模块目标

翻译模块用于完成一套文章级、分阶段的回译学习流程：

```text
导入原文
→ 固定分割
→ 完成整篇初译、关联与批注
→ 间隔一段时间
→ 完成整篇回译
→ 完成整篇原文／回译对照、关联与批注
→ 形成可浏览、引用和制作 Quiz 的句子记录
```

它不是普通双语翻译器，也不是把每个句子独立推进的闪卡系统。

核心约束：

- 学习流程由整篇文章统一推进。
- 固定分割后的每个单位是一条稳定记录。
- 同一阶段的全部记录完成后，整篇文章才能进入下一阶段。
- 修改翻译或回译只修改原记录，不产生 attempt、历史版本或新记录。
- 只有整篇文章完成最终对照后，其中的记录才进入句子记录浏览器。
- 短段落与长文章使用同一通道，不建立第二套学习模型。

---

## 2. 两个尺度

### 2.1 文章对象

文章对象负责：

- 标题
- 学习语言
- 原文来源
- 原文快照
- 固定分割
- 当前阶段
- 阶段进度
- 回译日期
- 全文渲染

“文章”可以是：

- 一篇完整文章
- 笔记中的一段选文
- 若干连续 block
- 一首诗
- 一个只有一句话的短段落

短文本只是较小的文章对象，不是另一种数据类型。

### 2.2 记录对象

文章固定分割后，每个单位形成一条稳定记录：

```text
Article
├─ R1
├─ R2
├─ R3
└─ ...
```

记录通常是一句话或一个分句，也可以是用户明确保留的多行诗句。

记录是翻译、回译、关联、批注和 Quiz 引用的承载单位。

---

## 3. 语言规则

创建文章时，目前只选择：

```text
English
日本語
```

这个语言字段规定：

- 原文语言
- 回译语言
- 与语言相关的分句、句子识别规则

中间翻译可以使用任何语言，不设置目标语言，也不限制为中文。

创建界面中不应出现“译文语言”选择。

---

## 4. 文章状态

用户可见状态：

```text
待整理
初译中
等待回译
该回译了
回译中
对照中
已完成
```

说明：

- “该回译了”“已过期 N 天”由计划日期实时计算，不需要单独持久化。
- “待整理”同时包含尚未开始切分和切分了一部分的文章，不增加“待开始”状态。
- “该回译了”“已过期 N 天”只是“等待回译”的实时显示，不是持久化状态。
- 固定分割全部完成后，由用户明确点击“完成整理，开始初译”，直接进入初译。
- 回译全部完成后，由用户明确点击“完成回译，开始对照”，直接进入对照；点击前原文始终隐藏。

状态转换：

```text
录入原文
  ↓
待整理
  ↓ 完成固定分割并明确开始
初译中
  ↓ 全部记录完成并确认初译
等待回译
  ↓ 到期或提前开始
回译中
  ↓ 全部记录完成并明确开始对照，原文重新显示
对照中
  ↓ 全部记录完成
已完成
```

---

## 5. 阶段门槛

### 5.1 文章统一推进

不允许同一篇文章出现：

```text
R1 已进入回译
R2 仍在初译
```

或者：

```text
R1 已开始对照
R3 尚未完成回译
```

用户可以在当前阶段内自由选择记录处理顺序，但不能跨阶段。

### 5.2 记录的阶段完成

每条记录在当前阶段由用户明确点击“完成本句”。

关联和批注可以为空。没有值得记录的关联时，不强迫用户制造内容。

阶段完成条件：

```text
初译阶段：
全部记录均有翻译，并被用户确认完成。

回译阶段：
全部记录均有回译，并被用户确认完成。

对照阶段：
全部记录均被用户确认已经完成对照。
```

---

## 6. 录入入口

### 6.1 从笔记选区录入

用户在 Obsidian 中选中文字，点击“录入翻译练习”。

不能立即创建并跳转。先显示快速确认窗口：

```text
┌────────────────────────────────────────────┐
│ 新建翻译练习                               │
│                                            │
│ 来源                                       │
│ 英语素材 / Proposal.md                     │
│ ┌────────────────────────────────────────┐ │
│ │ The proposal was ambitious. However... │ │
│ └────────────────────────────────────────┘ │
│                                            │
│ 语言                                       │
│ [ English ]  [ 日本語 ]                    │
│                                            │
│ 标题                                       │
│ [ The proposal was ambitious            ] │
│                                            │
│                         取消   保存   开始整理 │
└────────────────────────────────────────────┘
```

规则：

- 标题默认取原文开头的若干词或字符。
- 标题可以修改。
- 默认标题在窗口中由用户确认，而不是未经确认直接创建。
- 打开窗口时标题可默认全选，方便直接覆盖。
- 语言可以自动判断并预选，但必须明确显示并允许修改。
- `保存`：建立文章，状态为“待整理”，留在当前笔记。
- `开始整理`：建立同一文章并打开整理任务页。
- 两个按钮产生相同数据与状态，只区别是否立即跳转。
- 取消不产生任何文章或残留数据。

### 6.2 从文本练习浏览器新建

入口可以选择：

- 粘贴文本
- 当前笔记选区
- 从笔记选取连续文本或若干 block

原文出现后产生可修改的标题建议。

### 6.3 短段落

短段落走完全相同的通道：

```text
选中短段落
→ 确认标题与语言
→ 待整理
→ 固定分割
→ 完整阶段流程
```

不增加：

- 待开始状态
- 独立收集箱数据类型
- 直接进入句子记录浏览器的捷径

---

## 7. 固定分割

### 7.1 操作思想

原文首先以自然排版完整呈现。

用户在原文中选中一段，将其切为一个单位：

- 已切出的部分退灰。
- 未处理部分保持正文层级。
- 用户继续选择剩余部分，直到全部切完。

自动分割与手动分割使用同一模型：

- 自动分割只是批量切出剩余原文。
- 自动结果仍可在开始初译前重新整理。
- 多行诗句可以保持为一个单位。
- 换行不是强制单位边界。
- 一个自然段仍然保留为一个 block。

### 7.2 分割后的身份

分割确认后：

- 每个固定单位获得稳定记录 ID。
- 原文、记录顺序、分隔符和段落边界被锁定。
- 日常翻译、回译、关联和批注不会改变记录身份。

再次翻译只修改原记录。

### 7.3 分隔信息

每条记录保存 `separatorAfter` 或等价信息：

- 空格
- 标点之后的间隔
- 换行
- 段落边界

这样既能逐记录操作，也能重新拼出完整原文、翻译或回译。

### 7.4 整理任务页

```text
← 议案与证据                         已保存   ···
────────────────────────────────────────────────────

整理原文
已切出 3 个单位                         2 处待处理

                    P1
        The proposal was ambitious.
        However, the evidence remained limited.
        The committee decided to continue.

                    P2
        This second paragraph contains another unit.
        It provides context for testing paragraph navigation.

────────────────────────────────────────────────────
撤销  重做  自动分割     已选择 2 个单位
                         重新切分  合并  完成整理
```

视觉：

- 原文是页面主体，不放在大量小卡片里。
- block 标号位于安静的左侧沟槽。
- 已分割部分以低对比底色和细下边缘表示。
- 当前选择用唯一强调色。
- 操作集中在固定底栏。
- 不重复显示“结构预览”。

快捷键：

- `Ctrl+Z`：撤销
- `Ctrl+Shift+Z` / `Ctrl+Y`：重做
- `Esc`：取消选择
- `Ctrl+Enter`：全部切完后完成整理

---

## 8. 初译阶段

### 8.1 每条记录的内容

初译阶段同时完成：

- 翻译
- 原文片段与翻译片段的关联
- 关联或文本批注

这些是同一个阶段，不拆成多个文章阶段。

### 8.2 初译任务页

```text
← 议案与证据                    初译  04 / 11   ···
────────────────────────────────────────────────────

原文

I have always been puzzled, anyway,
by the economics of the thing,

────────────────────────────────────────────────────

翻译

我一直被这件事情的经济学所困惑，
anyway 应插在这里……

                                                156 字

────────────────────────────────────────────────────
批注与关联

the thing  ↔  这件事情
“thing” 是回指，不使用“this”。

────────────────────────────────────────────────────
上一项       保存为草稿       完成本句       下一项
```

视觉：

- 原文和翻译形成明确的上下或左右主从区域。
- 翻译编辑器是正文编辑面，不是一个巨大的默认 textarea 方框。
- 关联直接在句子中表现，不把文本拆成胶囊。
- 批注区贴近当前句子，不做独立管理后台。
- 当前记录位置和文章阶段清楚但克制。

---

## 9. 回译等待与日期

### 9.1 日期设置

初译全部完成后，文章进入等待回译。

完成页提供：

```text
初译完成

11 个单位已经处理

下次回译
[ 3 天后 ▾ ]    7 月 28 日

                    回到练习库
```

设置：

- 插件有默认回译间隔，例如 3 天。
- 每篇文章可以覆盖默认值。
- 支持 1 天、3 天、7 天和自定义日期。
- 以日期为粒度，不要求设置具体时间。

### 9.2 状态显示

```text
等待回译 · 还有 3 天
等待回译 · 明天
今日回译
已过期 4 天
```

排序优先级：

```text
已过期最久
→ 今日到期
→ 即将到期
→ 其他活跃文章
→ 已完成
```

可以修改日期或延后。

到期前可以保留次级“提前开始”操作，但不作为主要按钮。

---

## 10. 回译阶段

### 10.1 操作规则

回译逐固定记录进行：

```text
R1 → 写回译 → 完成本句
R2 → 写回译 → 完成本句
...
Rn → 写回译 → 完成本句
```

它不是连续全文编辑器。

回译时：

- 原文隐藏。
- 原文／翻译关联隐藏。
- 初译批注不作为提示展示。
- 只显示中间翻译和回译编辑区域。
- 回译语言由文章的 English / 日本語字段决定。

回译阶段允许添加批注：

- 可以边写边加。
- 可以写完后再加。
- 最终对照阶段仍可修改和补充。

### 10.2 回译任务页

```text
← 议案与证据                    回译  04 / 11   ···
────────────────────────────────────────────────────

翻译

我一直被这件事情的经济学所困惑，
总之……

────────────────────────────────────────────────────

回译

I have always been puzzled, anyway,
by the economics of it,

────────────────────────────────────────────────────

批注

[＋ 添加批注]

────────────────────────────────────────────────────
上一项       保存为草稿       完成本句       下一项
```

不能因为完成最后一句而立即显示原文。

最后一句完成后：

```text
回译完成
→ 文章仍处于“回译中”，页面出现“完成回译，开始对照”
→ 用户明确点击
→ 原文才重新出现
```

---

## 11. 对照阶段

### 11.1 每条记录的内容

对照阶段主要比较：

```text
原文 ↔ 回译
```

可以：

- 建立原文片段与回译片段的关联
- 写新的对照批注
- 修改和补充回译阶段批注
- 标出遗漏、误解、语序、冠词、复数、连接词、搭配等差异

中间翻译可以作为辅助信息访问，但不抢占原文／回译的主要层级。

### 11.2 对照任务页

```text
← 议案与证据                    对照  03 / 11   ···
────────────────────────────────────────────────────

S3

原文
and he would presumably give his wife
the money for her courses at the college,

回译
and he would presumably give his wife
money for her course at college,

────────────────────────────────────────────────────

关联与批注

the money     ↔  money
courses       ↔  course
the college   ↔  college

冠词和单复数均遗漏。

翻译                                              展开

────────────────────────────────────────────────────
上一项                完成对照                下一项
```

视觉：

- O/R 对齐是主结构。
- 即使完全相同的记录也保留并显示，保证文章覆盖完整。
- 差异标记精确到片段，不把整句涂成高亮块。
- 当前关联只使用一种强调色；其他关系保持低对比。

---

## 12. 不连续关联

### 12.1 关系模型

一个关联的任一侧都可以由多个不连续片段构成：

```text
would + give  ↔  会给

he + his wife  ↔  他 + 妻子
```

数据：

```text
Relation
├─ leftRanges[]
├─ rightRanges[]
└─ annotation
```

初译阶段：

```text
原文片段组 ↔ 翻译片段组
```

对照阶段：

```text
原文片段组 ↔ 回译片段组
```

### 12.2 操作

```text
1. 选择原文第一段
2. 继续选择原文第二段、第三段
3. 转到对侧选择一个或多个片段
4. 完成关联
```

进入关联捕获后，继续选择不要求一直按住 Ctrl。

浮动操作条：

```text
┌──────────────────────────────────────┐
│ 原文 2 段  ↔  翻译 1 段             │
│ would · give  ↔  会给                │
│              撤销上一段  取消  完成   │
└──────────────────────────────────────┘
```

规则：

- 片段不能跨固定记录。
- 同一关联可以包含多个断开片段。
- 相邻或重叠的本次选择自动合并。
- 继续遵守不允许不同关系相互重叠的约束。
- 已被其他关系占用的文字不能再次包裹。

### 12.3 视觉

- 同一关联中的片段使用相同下划线或轻底色。
- 不在断开的片段之间画跨文字连接线。
- 悬停任一片段时，同时强调同一关联的全部片段和批注。

示意：

```text
he would presumably give his wife money
   ─────             ────

他大概会给妻子钱
      ────
```

---

## 13. 全文渲染

工作操作仍然逐固定记录进行。

全文能力是额外查看功能，不改变已有操作方式。

完成若干记录后，可以按记录顺序和分隔信息派生：

- 翻译全文
- 回译全文
- 原文全文／回译全文对照

```text
回译全文

I have always been puzzled, anyway, by the economics
of it, because I would pay the dentist, and he would...
```

全文不能成为新的权威数据副本。编辑仍回到具体记录。

---

## 14. 三类主页面

```text
浏览器：寻找对象
文章页：查看一个文章对象的状态
任务页：完成当前阶段
```

此外还有完成成果使用界面：

```text
句子记录浏览器：使用已完成文章中的记录
```

### 14.1 完成阶段之后去哪里

完成阶段任务后：

- 整理完成：用户点击主按钮后直接进入初译。
- 初译完成：用户确认下次日期后进入等待回译文章页。
- 回译完成：用户点击主按钮后直接进入对照。
- 对照完成：进入文章完成页。

文章页显示：

- 已完成阶段
- 当前状态
- 下一步
- 文章级日期
- 各阶段进度

所有跨阶段动作都由用户明确点击；完成最后一条记录本身不自动推进。

---

## 15. 文章状态页

### 15.1 等待回译

```text
← 文本练习
────────────────────────────────────────────────────

议案与证据
English · 11 个固定单位
来源：英语素材 / Proposal.md

整理       11 / 11       完成
初译       11 / 11       完成
回译        0 / 11       7 月 28 日
对照        0 / 11

等待回译
还有 3 天

[修改日期]                              [提前开始]
```

### 15.2 已完成

```text
← 文本练习
────────────────────────────────────────────────────

议案与证据                                  已完成
English · 11 条记录
来源：英语素材 / Proposal.md

初译       11 / 11
回译       11 / 11
对照       11 / 11

[原文全文]  [翻译全文]  [回译全文]  [全文对照]

记录
S1  I have always been puzzled...       2 关联 · 1 批注
S2  because I would pay the dentist...  无批注
S3  and he would presumably...          3 关联 · 1 批注
...

[在句子记录浏览器中查看]
```

完成后文章对象仍然存在于文本练习浏览器。

---

## 16. 文本练习浏览器

它管理全部文章对象及其生命周期。

它回答：

```text
我有哪些文本正在处理？
现在分别做到哪一步？
哪篇已经到回译时间？
```

### 16.1 筛选

```text
全部
待整理
初译中
等待回译
该回译了
回译中
对照中
已完成
```

### 16.2 视图

```text
翻译练习                                             [新建]
──────────────────────────────────────────────────────────

全部  待整理  初译中  等待回译  回译中  对照中  已完成
                                               [搜索练习]

练习                   当前状态               当前进度
──────────────────────────────────────────────────────────

议案与证据             已过期 3 天             初译 11/11
Proposal.md                                    回译  0/11

夏日十四行诗           初译中                  7 / 14
诗歌摘录.md

こころ・冒頭           对照中                  4 / 8

短段落：经济学循环     待整理                  已切 2 个单位
Thyroid Diary.md
```

视觉：

- 一行是一篇文章，不是一条句子记录。
- 到期信息优先于普通更新时间。
- 不使用一个难以解释的总百分比替代不同阶段进度。
- 当前阶段是最醒目的状态，其他阶段退居次要层级。
- 已完成文章可以归档，但不会因此从句子记录浏览器删除。

---

## 17. 句子记录浏览器

只收录整篇文章已经完成最终对照的记录。

记录不会在某个句子提前完成时逐条进入。

```text
整篇文章完成最终对照
→ 文章中的全部记录一次进入句子记录浏览器
```

它回答：

```text
我已经积累了哪些可以复习、引用和制作 Quiz 的记录？
```

它不是待办事项列表，不显示任何流程状态；流程状态只属于文本练习浏览器。

### 17.1 主视图

```text
句子记录
────────────────────────────────────────────────────────────

[搜索原文、翻译、回译或批注]

English  日本語    来源    有批注    有关联    Quiz

记录列表                              当前记录
───────────────────────┬────────────────────────────────────
                       │
S1                     │ 原文
I have always been...  │ I have always been puzzled...
我一直被……             │
2 关联 · 1 批注        │ 翻译
                       │ 我一直被这件事情……
S2                     │
because I would pay... │ 回译
因为我会付钱……         │ I have always been puzzled...
无批注                 │
                       │ 原文／翻译关联
S3                     │ the thing ↔ 这件事情
and he would...        │
3 关联 · 1 批注        │ 原文／回译关联
                       │ the thing ↔ it
                       │
                       │ 批注
                       │ ...
                       │
                       │ 来源：议案与证据 / S1
───────────────────────┴────────────────────────────────────
```

记录可以继续修改：

- 修改翻译
- 修改回译
- 修改关联
- 修改批注

仍然是同一条记录，不产生版本。

未来可从这里：

- 制作 Quiz
- 在其他浏览页筛选和排序
- 在 Obsidian 其他语境中引用渲染
- 发起重新学习

Tag 系统的最终模型尚未确定，不在当前设计中固化分类结构。

---

## 18. Block 行内按钮

笔记中的行内按钮绑定文章 ID，不绑定具体任务页面。

行内内容表达：

```text
这段内容属于 article_xxx
```

而不是：

```text
打开初译页
打开回译页
```

### 18.1 渲染

```text
[翻译练习 · 待整理]
[初译 · 6/11]
[回译 · 还有 3 天]
[回译 · 已过期 2 天]
[对照 · 4/11]
[已完成 · 11 条记录]
```

### 18.2 点击行为

- 待整理、初译中、回译中、对照中：恢复当前任务页和上次记录。
- 等待回译：打开文章状态页。
- 已完成：打开文章完成页。
- 右键菜单始终可以选择“查看文章状态”。

按钮的稳定语义是：

```text
打开这篇翻译练习
```

阶段改变后不需要重写笔记中的行内引用。

---

## 19. 行内关联语法与数据

关联片段采用同一 ID 多次包裹的思路，允许一个端点不连续：

```text
he {{r17 would}} presumably {{r17 give}} his wife money
他大概{{r17 会给}}妻子钱
```

同一个 `r17` 的多个包裹共同构成一个关联。

原则：

- 行内语法提供稳定锚点和可编辑文本。
- 完整关系、批注、来源和状态保存在插件数据文件中。
- 数据文件是完整备份和索引来源。
- 行内渲染可以在 Obsidian 的其他笔记、浏览器和 Quiz 中复用。
- 插件渲染中仍允许其他 Obsidian 插件的可渲染语法继续工作。

精确的转义、损坏恢复和文本编辑同步规则仍需在正式实现前单独设计。

---

## 20. 建议数据结构

```text
Article
├─ id
├─ title
├─ language                  English | 日本語
├─ sourceReference
├─ sourceSnapshot
├─ stage
├─ stageStartedAt
├─ initialTranslationCompletedAt
├─ backTranslationDueAt
├─ backTranslationStartedAt
├─ comparisonStartedAt
├─ completedAt
├─ activeRecordId
└─ records[]

Record
├─ id
├─ order
├─ blockId
├─ sourceText
├─ separatorAfter
├─ translation
├─ translationRelations[]
├─ translationAnnotations[]
├─ translationComplete
├─ backTranslation
├─ backTranslationAnnotations[]
├─ backTranslationComplete
├─ comparisonRelations[]
├─ comparisonAnnotations[]
└─ comparisonComplete

Relation
├─ id
├─ leftField
├─ leftRanges[]
├─ rightField
├─ rightRanges[]
└─ annotationIds[]

Annotation
├─ id
├─ targetType               record | fragment | relation
├─ targetId
├─ text
└─ createdAt / updatedAt
```

不加入：

- 翻译 attempt 列表
- 回译版本历史
- 独立短段落类型
- 每条记录独立推进的文章阶段

内部可以保留普通撤销或数据恢复机制，但它不是用户可见的“版本模型”。

---

## 21. 全局视觉原则

### 21.1 层级

- 原文、翻译和回译正文始终是页面主体。
- 状态、编号和工具退居沟槽、顶栏或底栏。
- 通过排版、留白、位置和线条表达层级，不依赖大量胶囊、箭头和卡片。
- 不显示开发者式坐标、公式或无意义符号。

### 21.2 控件

- 控件使用统一的平面按钮体系。
- 原生 Obsidian 默认按钮外观不能混入成品界面。
- 强调色只用于当前任务、当前关联和主操作。
- 边界、分隔与次要状态使用中性色。
- 操作台可以粘在底部，但不能遮挡正文。

### 21.3 页面

- 浏览器以清晰列表为主，不做通用管理后台仪表盘。
- 任务页以编辑内容为主，不用装饰性大标题挤压空间。
- 文章页是稳定对象主页，不是第四个复杂编辑器。
- 空白应服务阅读和聚焦，不能变成无组织的大块死空间。

### 21.4 文案

- 不向用户解释他已经知道的学习方法。
- 不显示“开始后将锁定”等长期警告。
- 状态和主操作使用短文案。
- 具体页面名称可以继续打磨，不应反过来改变产品模型。

---

## 22. 核心快捷键

整理阶段：

```text
Ctrl+Z                 撤销
Ctrl+Shift+Z / Ctrl+Y  重做
Esc                    取消选择
Ctrl+Enter             完成整理
```

初译、回译、对照阶段：

```text
Ctrl+Enter             完成本句并进入下一项
Alt+↑                  上一项
Alt+↓                  下一项
Esc                    退出当前关联捕获／关闭浮层
Ctrl+Z                 当前编辑器内撤销
```

关联捕获：

```text
Esc                    取消本次关联
Ctrl+Z                 撤销上一段选择
Enter                  完成关联
```

快捷键需避开 Obsidian 编辑器已有关键行为，并允许后续配置。

---

## 23. 当前不应擅自确定的内容

以下内容尚未完成产品讨论，正式实现时不应自行扩展：

- Tag 系统的最终模型
- Quiz 的具体题型和调度
- 自动 diff 在最终对照中的权力边界
- 行内语法的完整转义和冲突处理
- 通知、系统提醒与外部日历
- 多个零散 block 是否允许 gather 为一篇文章
- 已完成文章重新打开阶段时的下游完成标记处理

---

## 24. 最小产品闭环

第一版完整闭环应当能够：

```text
从笔记选区录入
→ 确认标题和 English / 日本語
→ 手动或自动固定分割
→ 逐记录完成翻译、关联和批注
→ 设置回译日期
→ 到期后逐记录完成回译和批注
→ 逐记录完成原文／回译对照、关联和批注
→ 查看完成文章
→ 在句子记录浏览器中浏览全部完成记录
→ 从原始 block 的行内按钮重新访问文章
```

这一闭环完成前，不应优先扩张 Quiz、Tag 或其他学习模块。
